<div class="navbar bg-base-100">
  <div class="navbar-start">
    <a class="btn btn-ghost normal-case text-xl">1day Web Quiz</a>
  </div>
  <div class="navbar-center hidden lg:flex">
    <%= link_to '一覧', questions_path, class: "btn btn-ghost normal-case text-xl" %>
    <%= link_to '回答する', new_question_path, class: "btn btn-ghost normal-case text-xl" %>
  </div>
  <div class="navbar-end">
    <% if logged_in? %>
      <%= link_to 'Log out', logout_path, data: { turbo_method: :delete}, class: "btn" %>
    <% else %>
      <%= link_to 'Sign up', new_user_path, class: "btn" %>
    <% end %>
  </div>
</div>
