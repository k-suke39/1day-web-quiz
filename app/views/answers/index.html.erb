<div class="flex flex-col items-center mt-5">
  <% @answers.each do |answer|%>
    <h1 class="mt-3 text-2xl font-bold btn glass"><%= link_to answer.question.title, question_path(answer.question.id)%></h1>
  <% end %>
</div>